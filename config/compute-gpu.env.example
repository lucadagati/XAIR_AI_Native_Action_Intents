# Copy to config/compute-gpu.env (gitignored) and fill in locally.
export GPU_HOST=100.86.223.16
export GPU_USER=ml
export GPU_SSH_PASSWORD=   # prefer empty + SSH key via ssh-copy-id ml@$GPU_HOST
export GPU_SSH_IDENTITY_FILE=   # optional: path to private key (e.g. ~/.ssh/id_ed25519)
export OLLAMA_HOST=http://100.86.223.16:11434
export OLLAMA_MODEL=${OLLAMA_MODEL:-qwen2.5vl:7b}
